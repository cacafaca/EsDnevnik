<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          xmlns:d="http://xamarin.com/schemas/2014/forms/design"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          mc:Ignorable="d"
          x:Class="ProCode.EsDnevnikMob.DataTemplates.GradesViewCell"
          xmlns:converters="clr-namespace:ProCode.EsDnevnikMob.Converters">
    
    <ViewCell.View>
        <Grid x:Name="mainGradesGrid">
            <Grid.Resources>
                <converters:GradeColorConverter x:Key="gradeColorConverter" />
                <converters:AverageGradeConverter x:Key="averageGradeConverter" />
                <converters:DateConverter x:Key="dateConverter" />
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>
            <Label Text="{Binding Grade}"
                   x:Name="labelGradeBig"
                   FontSize="40"
                   FontAttributes="Bold"
                   Margin="5, 2, 5, 2"
                   TextColor="{Binding Grade, Converter={StaticResource gradeColorConverter}}"
                   Grid.Column="0" 
                   HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="2*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label Text="{Binding Course}"
                       x:Name="labelGradeCourse"
                       TextColor="{Binding Grade, Converter={StaticResource gradeColorConverter}}"
                       FontSize="Medium"
                       LineBreakMode="TailTruncation"
                       Grid.Row="0"
                       Grid.Column="0"
                       Grid.ColumnSpan="2" />
                <Label Text="{Binding GradeCategory}"
                       x:Name="labelGradeCategory"
                       LineBreakMode="TailTruncation"
                       FontAttributes="Italic"
                       VerticalTextAlignment="Center"
                       Grid.Column="0"
                       Grid.Row="1" />
                <Label Text="{Binding Date, Converter={StaticResource dateConverter}}"
                       x:Name="labelGradeDate"
                       LineBreakMode="TailTruncation"
                       HorizontalOptions="EndAndExpand" 
                       Grid.Column="1" Grid.Row="1"/>
                <Label Text="{Binding Note}"
                       x:Name="labelGradeNote"
                       LineBreakMode="WordWrap"
                       HorizontalOptions="StartAndExpand" 
                       Grid.Column="0" Grid.Row="2"/>
                <Label Text="{Binding Average, Converter={StaticResource averageGradeConverter}}"
                       x:Name="labelGradeAverage"
                       LineBreakMode="TailTruncation"
                       HorizontalTextAlignment="End"
                       FontAttributes="Bold"
                       HorizontalOptions="EndAndExpand"
                       VerticalTextAlignment="End"
                       MinimumWidthRequest="50" 
                       Grid.Column="1" Grid.Row="2"/>
            </Grid>
        </Grid>
    </ViewCell.View>
    
</ViewCell>